<%= form_for(@usern) do |f| %>
  <% if @usern.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usern.errors.count, "error") %> prohibited this usern from being saved:</h2>

      <ul>
      <% @usern.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
